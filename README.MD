### Требуемая инфрастуктура
Запущенный доступный Selenium GRID: отдельный сервет, Docker и т. п.

### Сборка, запуск
Перед сборкой нужно указать адрес доступа к хосту GRID в файле
`application.conf`.

Запуск `mvn clean test`.